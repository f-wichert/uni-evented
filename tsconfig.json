{
    "extends": "expo/tsconfig.base",
    "compilerOptions": {
        "strict": true,
        "baseUrl": "./src" // all paths are relative to the baseUrl
    },
    "typeRoots": ["src/index.d.ts"]
}
